const lalalas = document.querySelectorAll(".lalala");
console.log(lalalas[0].textContent);
console.log(lalalas[0].textContent);
console.log(lalalas[0].textContent);

for (let i = 0; i < 3; i++) {
  // document.querySelectorAll(".lalala")[i].textContent = "yes";
  lalalas[i].textContent = "나나";
}
